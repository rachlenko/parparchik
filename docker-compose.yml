services:
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 2s
      timeout: 5s
      retries: 10

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: /bin/sh
    command:
      - -c
      - |
        mc alias set local http://minio:9000 minioadmin minioadmin
        mc mb --ignore-existing local/public-bucket
        mc mb --ignore-existing local/private-bucket
        mc anonymous set download local/public-bucket
        echo "Buckets created and configured"

  parparchik:
    build:
      context: .
      dockerfile: Dockerfile.test
    ports:
      - "8080:8080"
    environment:
      PARPARCHIK_PUBLIC_BUCKET: public-bucket
      PARPARCHIK_PRIVATE_BUCKET: private-bucket
      PARPARCHIK_REGISTRY_MANIFEST_KEY: .parparchik/files.json
      S3_ENDPOINT: minio:9000
      S3_EXTERNAL_ENDPOINT: "localhost:9000"
      AWS_REGION: us-east-1
      AWS_ACCESS_KEY_ID: minioadmin
      AWS_SECRET_ACCESS_KEY: minioadmin
      PARPARCHIK_HOST: "0.0.0.0"
      PARPARCHIK_PORT: "8080"
    depends_on:
      minio-init:
        condition: service_completed_successfully
